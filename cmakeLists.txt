cmake_minimum_required(VERSION 3.13)

set(CMAKE_CXX_STANDARD 17)

set(ACRYLICEXE main.cpp)


#Platfrom Defination
set(MACOS FALSE)
set(WINDOWS FALSE)
set(LINUX FALSE)

if(${CMAKE_SYSTEM_NAME} MATCHES "Darwin")
    set(MACOS TRUE)
    include_directories(/usr/local/include)

elseif(${CMAKE_SYSTEM_NAME} MATCHES "Windows")
    set(WINDOWS TRUE)
    #We have to do this because windows doesn't havea central include directory
    #Vulkan
    include_directories(C:/SDKS/VulkanSDK/1.1.114.0/Include)
    #GLM
    include_directories(C:/SDKS/GLM0.9)
    #GLFW
    include_directories(C:/SDKS/GLFW3.3/glfw-3.3.bin.WIN64/include)
    #Load Modules
    set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_SOURCE_DIR}/cmake/Modules/")

elseif(${CMAKE_SYSTEM_NAME} MATCHES "Linux")
    set(LINUX TRUE)
    message(AUTHOR_WARNING "Building on Linux has not been tested. Please use macOS or Windows to build")
    include_directories(/usr/local/include)

else()
    message(FATAL_ERROR "Unable to detect operating system. Build halted")
endif()

#Lib Includes
find_package(vulkan REQUIRED)
find_package(glfw3 3.3 REQUIRED)

add_executable(Acrylic2D ${ACRYLICEXE})

target_link_libraries(Acrylic2D Vulkan::Vulkan)

if(${WINDOWS})
    target_link_libraries(Acrylic2D ${GLFW3_LIBRARY})#For somereason this only works on AMD64 for windows. Not AMD64 for mac
else()
    target_link_libraries(Acrylic2D glfw)
endif()